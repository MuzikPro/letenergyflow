/**
 * Build-time constants injected by Vite's `define`.
 *
 * `__SW_BUILD__` is a per-build id, used to give the service worker's cache a
 * name that changes when the app does — see `vite.config.ts` and `public/sw.js`.
 */
declare const __SW_BUILD__: string;
